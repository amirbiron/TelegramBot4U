# CLAUDE.md — הנחיות פיתוח לפרויקט בוט ה-Secretary (בוט לכל לקוח)

> מסמכי העוגן: `PLAN.md` (החלטות ארכיטקטורה ורציונל), `ROADMAP.md` (ביצוע משימה-משימה), `docs/verification_log.md` (ממצאי משימות האימות V1–V5). הקובץ הזה נגזר מ-`CLAUDE.md` של `ai-business-bot` והותאם לפרויקט: כללים שנשענו על RAG, דקורטורים, מקלדות, WhatsApp ומסך הסכמה — הוחלפו בכללי הערוץ החדש. השאר חל כלשונו.

## תהליך עבודה

1. **פתרונות שורש בלבד** — כלל קבוע: לכל בעייה תמיד נחפש פתרון שורש ולא טלאי.
2. **הנחות לא מאומתות** — קוד שנשען על הנחה מ-V1–V5 שטרם אומתה מפנה אליה בהערה, ולא בונים עליה שכבות נוספות עד שתאומת ותתועד ב-`verification_log.md`.

## שפה

בצ'אט איתי בעברית, סיכומי PR, תיאורי commit, הערות בקוד — **בעברית**. שמות משתנים, פונקציות וטבלאות — באנגלית.

## ארכיטקטורה

- **מבנה:** מודולים בשורש (`tenancy.py`, `control_plane.py`, `database.py`...) + חבילות `bot/`, `core/`, `services/`, `admin/`, `utils/`, `memory/`. **אין** namespace aliases (‏`ai_chatbot/` לא משוחזר) — בהעתקת קוד וטסטים מהריפו הישן מנרמלים imports ויעדי `patch` (‏`ai_chatbot.X` ⇒ ‏`X`).
- **DB:** ‏SQLite עם WAL. חיבור אך ורק דרך `get_connection()` שפותח את הקובץ לפי `tenancy.tenant_db_path()`. אסור `sqlite3.connect` ישיר מול נתיב.
- **ערוץ יחיד:** ‏telegram_business. אין WhatsApp, אין Meta, אין widget — ואין להחזיר אותם.
- **LLM:** שכבה A — פרסונה פר-ערוץ (`build_system_prompt`); שכבה B — **בסיס הידע המלא** דרך `kb_service.get_kb_context()`. אין RAG, אין chunks, אין FAISS. ‏retrieval עתידי — רק מאחורי אותה פונקציה, ורק אחרי דיון מפורש.
- **סדר הפרומפט מקובע** (prompt caching): פרסונה ← KB ← הגדרות tenant ← זיכרון לקוח/סיכום ← היסטוריה ← שאלה. תוכן תנודתי לא נכנס לפני ה-KB. כשמזריקים תוכן חדש לפרומפט — לקרוא את כל ההודעות כשלם ולוודא שאין הוראות סותרות.

## Multi-tenant — כללים מחייבים

- **זהות עסקית** (שם/טלפון/כתובת) — אך ורק דרך `config.get_business_config()` בזמן ריצה. אסור `from config import BUSINESS_NAME` (הערך קופא ב-import).
- **קביעת tenant** — רק בנקודות כניסה (route של webhook, ‏`before_request` בפאנל, לולאות schedulers, ‏CLI) דרך `tenant_context()` / `set_current_tenant()`. קוד עמוק קורא `get_current_tenant()` ולא מנחש. מעבר בין threads לא מעביר את ה-context — מעבירים את ה-tenant כפרמטר וקובעים מחדש.
- **state ברמת מודול** (cache/dict/singleton) — מפתח tenant מהיום הראשון (דוגמה חיה: ה-cache של `kb_service`), או להיצמד ל-DB.
- ‏`TENANCY_STRICT=true` בפרודקשן; טסטים משתמשים בו לאיתור נתיבים לא מכוסים.

## DB

- טבלה חדשה: לזהות natural key ולהוסיף `UNIQUE`. ‏upserts עם `INSERT OR REPLACE` (חיבור מחדש / seed שניתן לדריסה).
- `init_db()` מריץ executescript ואז `run_migrations()`. עמודה חדשה לטבלה **קיימת** — ‏`_ensure_column` ב-`migrations.py`. אינדקס או constraint שתלויים בעמודה החדשה — **ב-migrations.py בלבד**: אם ייכנסו ל-executescript, ב-DB קיים הם יקרסו כי העמודה עוד לא קיימת (המיגרציה רצה אחרי). הבאג עובר CI על DB ריק ונתפס רק בפרודקשן.
- ‏`migrate_all_tenants()` רץ בכל עליית תהליך על כל tenant פעיל. אל תניח ש-tenant DB בגרסת סכימה מסוימת בלי שהלולאה רצה עליו.
- שתי פונקציות שחולקות לוגיקת סינון (`get_X`/`count_X`) — ‏helper פנימי משותף. שכפול WHERE מזמין סטייה שקטה.
- לוגיקה תלוית זמן — טבלת תרחישים לפני כתיבה (חלון 24 שעות, מעבר יום, ‏timeout השתקה, גבולות שנה).

## הערוץ — כללי ברזל

- **נקודת כניסה אחת ללקוח:** ‏`on_business_message`. ה-guards ליניאריים ובסדר קבוע: connection לא מוכר ⇒ עצירה; זיהוי בעלים אנושי (`from.id == owner_user_id` **וגם** `not is_from_offline` **וגם** `sender_business_bot is None`) ⇒ ‏takeover ושמירה; שמירת הודעה נכנסת (תמיד, גם בהשתקה); ‏block/השתקה ⇒ שקט; ‏rate limit ⇒ שקט ללקוח + התראה לבעלים; ‏`can_reply` חסר ⇒ שקט + התראה. **אין דקורטורים** — הכלל הישן ("דקורטור על כל handler") מוחלף בכלל: אסור נתיב חדש שפונה ללקוח מחוץ לצינור הזה.
- **ללקוח הסופי לעולם אין:** מקלדות, פקודות, הודעות מערכת ("אנא המתן", "בעל העסק הצטרף"), או טוקן `[HANDOFF]` שדלף. שתיקה עדיפה על הסגרת אוטומציה.
- **כל קריאה יוצאת** בצ'אט לקוח נושאת `business_connection_id`. ה-shortcuts של PTB (‏`reply_text` על `business_message`) עושים זאת אוטומטית; קריאת `Bot` ידנית — להעביר במפורש.
- ‏`set_webhook` של בוט-בן חייב `allowed_updates` שכולל את ארבעת סוגי עדכוני ה-Business; של הבוט המנהל — את `managed_bot`. שכחה = דממה שקטה בלי שגיאה.
- **חלון 24 השעות:** כל הודעה נכנסת מעדכנת `users.last_inbound_at`. כשל שליחה מסווג (חלון סגור / אין הרשאה / אחר) — סימון + התראה לבעלים, לא retry עיוור. אין פנייה יזומה ללקוח מחוץ לחלון — זו מגבלת API, לא העדפה.
- **Handoff:** הטוקן `[HANDOFF]` בתחילת התשובה, ‏`startswith` בלבד, ‏`strip_handoff_marker` לפני כל שליחה ללקוח. אסור fuzzy detection. ההסלמה השלישית = ‏handoff שקט לבעלים בצ'אט הבוט-הבן, לא "מעביר אותך לנציג".
- **rate limit** (`check_rate_limit` + ‏`record_message`) על כל נתיב LLM שמקורו במשתמש קצה.

## קוד

- ‏`except Exception: pass` אסור — תמיד `logger.error(...)`.
- לולאת I/O על רשימת פריטים (digest על כל ה-tenants, מחיקות מרובות) — ‏try/except עם לוג **פר-פריט**; כשל אחד לא עוצר את השאר.
- ‏asyncio: ‏`Bot` שנוצר מחוץ ל-Application דורש `initialize()`/`shutdown()`. ‏Future מ-`run_coroutine_threadsafe` — תמיד `add_done_callback`, ובודקים `cancelled()` **לפני** `exception()`. ‏cleanup ב-finally עטוף בנפרד כדי לא לדרוס תוצאה שכבר נכתבה.
- ‏JSON מה-LLM (רלוונטי לחילוץ זיכרון לקוחות): הפרומפט חייב לכלול את שמות השדות המדויקים + דוגמה מלאה אחת לפחות; טסט אוכף התאמת פרומפט⇄סכמה. שדה חדש בסכמה = עדכון הפרומפט באותו commit.
- ‏Templates: ‏datetime מה-DB רק דרך `il_datetime` / `il_date` / `il_datetime_local`; אסור `{{ value }}` חשוף. ‏HTMX: אלמנטים קשורים נמחקים/מוחלפים יחד בקונטיינר משותף. ‏route חדש בלי UI שקורא לו באותו commit — אסור.
- ‏LIKE עם קלט משתמש (כולל `/kb/search`) — ‏escaping של `_` ו-`%`.
- ‏`.env.example` מתעדכן בכל commit שמוסיף משתנה סביבה; ‏`docs/client_guide.md` מתעדכן בכל שינוי ב-seed, בהגדרות, או בזרימת החיבור.

## פרטיות — תיקון 13, מותאם לערוץ

- **אין מסך הסכמה ללקוח הסופי — ואי אפשר שיהיה** (אין `/start`, אין כפתורים). הבסיס המשפטי שונה מהריפו הישן: ה-tenant הוא בעל המאגר וחב בחובת היידוע; אצלנו — הסכם עיבוד נתונים, שורת גילוי בצ'אט חדש (ברירת מחדל דלוקה), וכלל השקיפות (לא מכחישים אוטומציה כשנשאלים ישירות). לכן שמירת הודעות **אינה מותנית** בהסכמת משתמש הקצה — זו התכתובת של בעל החשבון. הכלל הישן "אין כתיבת PII לפני consent" לא חל כאן; מה שכן חל: מזעור (טקסט בלבד, בלי מדיה בשלב 1).
- כל טבלה חדשה — שורה ב-`docs/privacy_data_matrix.md` **באותו commit** (גם טבלאות בלי PII — שורה קצרה עם שיקול אבטחה). טבלה חדשה עם `user_id` — עדכון `delete_user_data` באותו commit; הטסט הלולאתי על סכימת ה-DB אוכף זאת.
- ‏`deleted_business_messages` = מחיקת העותקים מיידית, כולל נגזרות (עובדות זיכרון, קלט לסיכומים) + רישום ב-consent_ledger.
- בקשת מחיקה בשפה חופשית מהלקוח ("תמחקו את המידע שלי") מזוהה כ-intent וממופה ל-`delete_user_data` עם אישור לבעלים.
- ‏PII בלוגים אסור: טלפונים ממוסכים (`+972****1234`), בלי תוכן הודעות ברמת INFO, בלי מפתחות (`sk-...` מסונן).

## דפוסים קריטיים — security / data-loss (חל תמיד)

עשרת דפוסי ה-security ושישה הדפוסים האוניברסליים מ-`CLAUDE.md` של הריפו הישן חלים כלשונם — מעתיקים את `CRITICAL-PATTERNS.md` ואת `BY-STACK/` לריפו הזה בשלב 0. הרלוונטיים ביותר כאן: ‏XFF לא נקרא ישירות ב-rate limiter של ההתחברות; ‏LIKE escaping; ‏PII/secrets לעולם לא ב-response או בלוג; ‏OTP/token נשמר לפני שנשלח (fail closed); פאנל שנקשר ל-`0.0.0.0` דורש אימות לפני כל route.

## פקודות

```bash
python main.py            # הכל: בוטים + אדמין
python main.py --admin    # אדמין בלבד
python main.py --seed     # tenant דמו + KB לדוגמה
python -m pytest tests/ -v
```

---

## עדכון בטלגרם — לפי בקשה

כשהמשתמש מבקש בתחילת משימה "עדכן אותי בטלגרם" / "תשלח לי בטלגרם כשתסיים"
(או ניסוח דומה) — בסיום המשימה שלח הודעת סיכום קצרה בעברית דרך Telegram Bot API:

- שולחים עם `curl` ל-`sendMessage`, דרך משתני הסביבה
  `TELEGRAM_BOT_TOKEN` ו-`TELEGRAM_CHAT_ID`.
- מעבירים את הטקסט עם `--data-urlencode "text=..."` (מטפל נכון בעברית ובתווים מיוחדים).
- כותבים את גוף ההודעה לקובץ ב-scratchpad ואז `text=$(cat file)` —
  כדי לא להתעסק עם escaping של shell על טקסט רב-שורתי.
- **degradation:** אם אחד המשתנים חסר — אל תיכשל ואל תעצור את המשימה;
  דווח בצ'אט "לא נשלח לטלגרם (משתנה X חסר)" והמשך כרגיל.

### עיצוב (אופציונלי)
כברירת מחדל שלח **בלי** `parse_mode` — טקסט גולמי, בטוח לכל תו.
רק אם צריך מודגש/נטוי/קישור לחיץ הוסף `--data-urlencode "parse_mode=HTML"`,
ואז — לפי כלל 6 — **חובה** `html.escape` על כל תוכן שמגיע ממקור חיצוני
(DB/API/פלט כלים), אחרת `< > &` ישברו את ההודעה.
